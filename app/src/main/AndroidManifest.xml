<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tercochistusveritaquevia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.MisterioActivity"></activity>
        <activity android:name=".Activity.CreioActivity" />
        <activity android:name=".Activity.SalveRainhaActivity" />
        <activity android:name=".Activity.AgradecimentoActivity" />
        <activity android:name=".Activity.TercoActivity" />
        <activity android:name=".Activity.OferecimentoActivity" />
        <activity android:name=".Activity.SelecaoActivity" /> <!-- <activity android:name=".Activity.SplashScreenActivity" /> -->
        <activity android:name=".Activity.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>